// Package tui implements AIR's Bubble Tea dashboard: current provider and
// task, elapsed time, provider queue, checkpoint count, cost, tokens, logs,
// progress, and keyboard shortcuts.
package tui
