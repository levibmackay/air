// Package summarizer compresses a checkpoint into a concise resume prompt:
// completed work, remaining tasks, files changed, current git diff, known
// issues, and latest output — never the full conversation history.
package summarizer
